import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'pe-login-styles',
  template: ``,
  styleUrls: ['./login-styles.component.scss'],
  encapsulation: ViewEncapsulation.None,
})
export class LoginStylesComponent {
}
