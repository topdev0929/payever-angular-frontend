import { NotServerRenderDirective } from './not-server-render.directive';

describe('NotServerRenderDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new NotServerRenderDirective();
  //   expect(directive).toBeTruthy();
  // });
});
