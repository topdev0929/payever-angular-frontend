import { OnlyServerRenderDirective } from './only-server-render.directive';

describe('OnlyServerRenderDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new OnlyServerRenderDirective();
  //   expect(directive).toBeTruthy();
  // });
});
